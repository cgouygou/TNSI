# Corrections BNS 2026

<!-- !!! example "sujet 23"
    ??? success "Correction Q1"
        ```python
        
        ```

    ??? success "Correction Q2"
        ```python

        ```

    ??? success "Correction Q3"
        ```python

        ``` 
    
    ??? success "Correction Q4"
    ```python

    ``` -->

!!! example "sujet 18"
    
    ??? success "Correction Q1"
        ```python
        def temperature_moyenne(zone, donnees):
            somme_temperatures = 0
            n = 0
            for d in donnees:
                if d['zone'] == zone:
                    somme_temperatures += d['temperature']
                    n += 1
            if n == 0:
                return None
            else:
                return somme_temperatures / n
        ```

    ??? success "Correction Q2"
        Petite incohérence sur le nom de la fonction entre l'énoncé (`#!py detecter_anomalies`) et le fichier fourni (`#!py detection_anomalies`).

        ```python
        def detection_anomalies(zone, seuil, donnees):
            t = temperature_moyenne(zone, donnees)
            liste_dates = []
            for d in donnees:
                if d['zone'] == zone and abs(d['temperature'] - t) > seuil:
                    liste_dates.append(d['date'])
            return liste_dates
        ```
        **Remarque:** l'exemple donné dans l'énoncé n'est pas correct, il doit renvoyer une liste vide avec l'exemple `#!py donnees` précédent.

        Un autre exemple (correct):

        ```python
        >>> detection_anomalies('Societe', 0.1, donnees)
        ['2020-01-15', '2020-01-16']
        ```
        

    ??? success "Correction Q3"
        Tous les test passent et je ne vois pas d'erreur dans le code. Argl...
        
        ```python
        def test_zone_inexistante():
            """
            Test 1 : Tester une zone qui n'existe pas

            À compléter:
            1. Appeler evolution_par_decennie avec une zone inexistante
            2. Vérifier que le résultat est un dictionnaire vide
            """
            assert evolution_par_decennie('Tahiti', donnees_test) == {}


        def test_une_seule_decennie():
            """
            Test 2: Tester une zone avec données sur une seule décennie

            À compléter:
            1. Appeler evolution_par_decennie avec la zone appropriée
            2. Vérifier que le résultat ne contient qu'une seule décennie (2020)
            3. Vérifier la température moyenne
            """
            assert len(evolution_par_decennie('Marquises', donnees_test)) == 1
            assert evolution_par_decennie('Marquises', donnees_test)[202] == 26.0


        def test_plusieurs_decennies():
            """
            Test 3 : Tester une zone avec données sur plusieurs décennies

            À compléter:
            1. Appeler evolution_par_decennie avec la zone appropriée
            2. Vérifier que le résultat contient bien les clés 2010 et 2020
            3. Vérifier que les températures moyennes sont cohérentes
            """
            resultat = evolution_par_decennie('Societe', donnees_test)
            assert 201 in resultat and 202 in resultat
            assert resultat[201] == 27.0 and resultat[202] == 28.5

        ``` 

    ??? success "Correction Q4"
    ```python

    ```




!!! example "sujet 19"
    ??? success "Correction Q1"
        ```python
        def est_en_penurie(liste_reservoirs, nom_reservoir):
            for reservoir in reservoirs:
                if reservoir["nom"] == nom_reservoir:
                    taux = reservoir["volume"] / reservoir["capacite"]
                    return taux < 0.2
        
        # tests (non demandés)
        assert est_en_penurie(reservoirs, "Motuavai")
        assert not est_en_penurie(reservoirs, "Nuiavai")
        ```


    ??? success "Correction Q2"
        ```python
        def volume_par_district(reservoirs):
            dico_districts = {}
            for reservoir in reservoirs:
                nom_district = reservoir["district"]
                if nom_district in dico_districts:
                    dico_districts[nom_district] += reservoir["volume"]
                else:
                    dico_districts[nom_district] = reservoir["volume"]
            return dico_districts
        ```

    ??? success "Correction Q3"
        ```python
        assert len(reservoirs) > 0
        assert volume_moyen(reservoirs) <= max([reservoir["volume"] for reservoir in reservoirs])
        assert volume_moyen([{"volume": 50000},{"volume": 50000}]) == 50000
        ```

        Pour corriger la fonction `#!py volume_moyen`, il faut envisager le cas où la liste de réservoirs est vide, et diviser par le nombre correct de réservoirs.
        ```python 
        def volume_moyen(reservoirs):
            """
            Renvoie le volume moyen d'eau disponible dans les réservoirs.
            """
            if len(reservoirs) == 0:
                return 0
            somme_totale = 0
            for r in reservoirs:
                somme_totale += r["volume"]
            moyenne = somme_totale / len(reservoirs)
            return moyenne

        ```
        

    ??? success "Correction Q4"
        Je ne comprends pas ce qui est attendu...
        ```python

        ```





!!! example "sujet 20"
    ??? success "Correction Q1"

        ```python
        def calculer_empreinte(utilisateur):
            empreinte = 0
            for activite, usage in utilisateur.items():
                empreinte += EMISSIONS[activite] * usage
            return empreinte

        assert calculer_empreinte(utilisateur1) == 7490
        ```

    ??? success "Correction Q2"
        ```python
        def classer_par_impact(utilisateur):
            dict_impact = {'fort': [], 'moyen': [], 'faible': []}
            for activite, usage in utilisateur.items():
                emissions = EMISSIONS[activite] * usage
                if emissions >= 1000:
                    dict_impact['fort'].append(activite)
                elif emissions >= 200:
                    dict_impact['moyen'].append(activite)
                else:
                    dict_impact['faible'].append(activite)
            return dict_impact

        assert classer_par_impact(utilisateur2) == {'fort': ['streaming_hd'],
                                                    'moyen': ['emails_simples'],
                                                    'faible': ['recherches']}
        ```

    ??? success "Correction Q3"
        Il faut tester les cas où l'activité est bsente chez un utilisateur ou les deux.
        
        ```python
        def test_comparer():
            diff = comparer(utilisateur4, utilisateur5)
            assert diff['emails_simples'] == -200  # (50-100) * 4
            assert diff['recherches'] == 350     # (100-50) * 7
            # Ajouter vos tests ci-dessous avec justifications
            assert diff['streaming_hd'] == 0  # activité absente chez les deux utilisateurs
            diff = comparer(utilisateur2, utilisateur4)
            assert diff['streaming_hd'] == -500  # (0-5) * 100; activité absente chez un utilisateur
        ```

    ??? success "Correction Q4"
        Si une activité est absente chez l'utilisateur 1, cela entraîne une division par 0. On peut donc plutôt envoyer une valeur par défaut, `#!py None` par exemple.

        ```python
        def comparer_v2(u1, u2):
            """Compare les émissions de deux utilisateurs pour toutes les activités.
            Renvoie un dictionnaire avec, pour chaque activité, l'écart des émissions
            sous forme de pourcentage, en proportion de la première émission."""
            ecarts = {}
            for activite in EMISSIONS:
                quantite1 = 0
                quantite2 = 0
                if activite in u1:
                    quantite1 = u1[activite]
                if activite in u2:
                    quantite2 = u2[activite]
                emission1 = quantite1 * EMISSIONS[activite]
                emission2 = quantite2 * EMISSIONS[activite]
                if emission1 == 0:
                    ecarts[activite] = None
                else:
                    ecarts[activite] = (emission2 - emission1)/emission1 * 100
            return ecarts
        ```








!!! example "sujet 21"
    <!-- Énoncé ambigu àmha à la question 1, mais vire la remarque si c'est moi qui ne sait pas lire. -->
    ??? success "Correction Q1"
        Contrairement à ce que sous-entend l'énoncé, on doit conserver une valeur de l'attribut `#!py self.niveau` entre 0 et 4 comme dans le tableau, cette valeur servant  ensuite d'indice pour récupérer le délai correspondant dans la liste `#!py DELAIS` et non affecter  directement le délai comme valeur à l'attribut `#!py self.niveau`.

        ```python
        def traiter_reponse(self, succes):
            if succes:   # équivalent à if succes == True:
                self.niveau += 1
            else:
                self.niveau = 0
            self.date_prochaine = date_future(DELAIS[self.niveau])
        ```
        

    ??? success "Correction Q2"
        La liste peut se construire par ajouts successifs, ou par compréhension.

        ```python
        def extraire_cartes_du_jour(paquet, date_jour):
            cartes_du_jour = []
            for carte in paquet:
                if carte.date_prochaine <= date_jour:
                    cartes_du_jour.append(carte)
            return cartes_du_jour

        def extraire_cartes_du_jour(paquet, date_jour):
            return [carte for carte in paquet if carte.date_prochaine <= date_jour]
        ```

    ??? success "Correction Q3"
        L'erreur réside dans le fait d'ajouter des cartes à la liste `#!py a_renforcer` lorsqu'on trouve une nouvelle valeur minimale: on conserve donc les cartes de l'ancienne valeur minimale. Pour rectifier, il faut réinitialiser cette liste avec seulement la carte correspondante.

        ```python
        def extraire_cartes_a_renforcer(paquet):
            """
            Parcourt le paquet et renvoie la liste des cartes ayant le 
            niveau d'avancement le plus faible.
            """
            if len(paquet) == 0:
                return []

            niveau_min = paquet[0].niveau
            a_renforcer = []

            for carte in paquet:
                if carte.niveau < niveau_min:
                    niveau_min = carte.niveau
                    a_renforcer = [carte]  # ligne modifiée
                elif carte.niveau == niveau_min:
                    a_renforcer.append(carte)

        return a_renforcer
        ```



!!! example "sujet 22"
    <!-- Bon je comprends pas la consigne de la question Q1.... -->
    ??? success "Correction Q1"
        De nombreuses méthodes existent pour passer du binaire au décimal. En voici une.

        ```python
        def bin2dec(tuple_binaire):
            decimal = 0
            for bit in tuple_binaire:
                decimal = 2*decimal + bit
            return decimal
        ```

        On peut donc ensuite utiliser cette fonction pour obtenir la chaîne de caractère du QR code de la figure 1:

        ```python 
        # implémentation du QR Code de la figure 1:
        qrcode_fig1 = ascii.figure1

        chaine = ''
        for t in qrcode_fig1:
            chaine += ascii.dict_ascii[bin2dec(t)]
        ```

        La variable `#!py chaine` contient `#!py 'M.Hara'`.


    ??? success "Correction Q2"
        ```python
        def qrcode2dec(qr):
            return [bin2dec(t) for t in qr]
        ```

        On peut alors écrire le test suivant:

        ```python
        assert "".join([ascii.dict_ascii[n] for n in qrcode2dec(qrcode_fig1)]) == 'M.Hara'
        ```

    ??? success "Correction Q3"
        On obtient une `#!py KeyError` car le dictionnaire ne possède pas de clé `#!py 233`. Il faut donc vérifier que la clé existe dans le dictionnaire `#!py table_ascii` avant d'y accéder:

        ```python
        def dec2str(liste_dec):
            """ entrée: liste d'entiers décimaux
                sortie: chaine de caractère formée des caractères correspondant
                de la table ascii """
            table_ascii = ascii.dict_ascii
            chaine = ""
            for entier in liste_dec:
                if entier in table_ascii:   # ligne ajoutée
                    chaine += table_ascii[entier]
            return chaine
        ```

    ??? success "Correction Q4"
        Avec le code de la fonction `#!py str2qrcode`, les tuples ne contiennent pas tous 8 bits. Il faut donc ajouter autant de `#!py 0` que nécessaire *en début de liste*.
        
        Par exemple:

        ```python
        def str2qrcode(message):
            """
            Convertit une chaine de caractères en liste de tuples binaires.
            """
            qrcode = []
            table_inverse = {valeur: cle for cle, valeur in ascii.dict_ascii.items()}

            for caractere in message:
                entier = table_inverse.get(caractere, 63)
                binaire_str = bin(entier)[2:]
                ligne = [int(bit) for bit in binaire_str]
                ligne = (8 - len(ligne))*[0] + ligne
                qrcode.append(tuple(ligne))

            return qrcode

            assert str2qrcode('M.Hara') == ascii.figure1
        ```


!!! example "sujet 23"
    En consultant le fichier `transmission.py`, on remarque qu'il s'agit de méthodes à compléter dans la classe `Transmission`, en utilisant donc la syntaxe de la POO. Il n'y a donc pas de paramètre `trame`... ni de valeur à renvoyer : il faut affecter les valeurs aux attributs de la classe. Ce sont donc des *setters*.

    ??? success "Correction Q1"
        On remarque que la classe `#!py Transmission` comporte des *getters*, il est donc préférable de les utiliser pour les tests plutôt que d'accèder directement aux attributs (privés, d'où l'utilisation du `#!py _`).

        ```python
        def decoder_temperature(self):
            temp_binaire=int(self._trame[16:28], 2)
            self._temperature = (temp_binaire - 900) / 10

        def decoder_humidite(self):
            humidite_dcb = self._trame[28:36]
            self._humidite = int(humidite_dcb[:4], 2)*10 + int(humidite_dcb[4:], 2)

        # ==========
        # Tests
        # ==========

        t = Transmission("0010101011001000010010001100011000101101")
        assert t.get_temperature() == 26.4
        assert t.get_humidite() == 62
        ```

    ??? success "Correction Q2"
        On vérifie pour chaque bloc de la trame la parité (en comptant les `#!py '1'`  dans le bloc) et on la compare on bit de contrôle.

        ```python
        def est_valide(self):
            blocs_trame = [self._trame[0:8], self._trame[8:16], self._trame[16:28], self._trame[28:36]]
            bits_controle = self._trame[36:40]
            for i in range(4):
                nombre_de_1 = 0
                for bit in blocs_trame[i]:
                    if bit == '1':
                        nombre_de_1 += 1
                if nombre_de_1 % 2 != int(bits_controle[i]):
                    return False
            return True 
        ```
        Autre version avec une fonction intégrée de calcul de parité d'un bloc:

        ```python
        def est_valide(self):
            def parite(bloc):
                nombre_de_1 = 0
                for bit in bloc:
                    if bit == '1':
                        nombre_de_1 += 1
                return nombre_de_1 % 2

            blocs_trame = [self._trame[0:8], self._trame[8:16], self._trame[16:28], self._trame[28:36]]
            bits_controle = self._trame[36:40]
            for i in range(4):
                if parite(blocs_trame[i]) != int(bits_controle[i]):
                    return False
            return True 
        ```


    ??? success "Correction Q3"
        On constate une `#!py IndexError` lors de l'appel à la méthode `#!py est_valide`. Après analyse du fichier `#!py data.txt`, on constate que certaines trames ne sont pas valides car elles ne contiennent pas 40 bits.

        Il suffit donc d'ajouter un test sur la longueur de la trame au début de la méthode `#!py est_valide`.

        ```python
        def est_valide(self):
            def parite(bloc):
                nombre_de_1 = 0
                for bit in bloc:
                    if bit == '1':
                        nombre_de_1 += 1
                return nombre_de_1 % 2

            if len(self._trame) != 40:
                return False

            blocs_trame = [self._trame[0:8], self._trame[8:16], self._trame[16:28], self._trame[28:36]]
            bits_controle = self._trame[36:40]
            for i in range(4):
                if parite(blocs_trame[i]) != int(bits_controle[i]):
                    return False
            return True 
        ```

        Mais cela ne résout pas les erreurs d'encodage des températures...

